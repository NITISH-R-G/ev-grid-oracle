# Assets

- **`validation_output.txt`** — generated by [`validate-submission.sh`](../validate-submission.sh) at the repo root (log of `pytest` and optional `openenv validate`). Safe to regenerate before demos; do not commit secrets.
